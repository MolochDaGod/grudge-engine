{
  "name": "grudge-engine",
  "private": true,
  "workspaces": [
    "shared",
    "front",
    "back"
  ],
  "scripts": {
    "build:shared": "pnpm --filter @grudge/shared run build",
    "dev:shared": "pnpm --filter @grudge/shared run dev",
    "build:back": "pnpm --filter @grudge/back run build",
    "dev:back": "pnpm --filter @grudge/back run dev",
    "build:front": "pnpm --filter @grudge/front run build",
    "dev:front": "pnpm --filter @grudge/front run dev",
    "build-vercel": "pnpm run build:shared && pnpm run build:front",
    "build": "pnpm run build:shared && pnpm run build:back && pnpm run build:front",
    "dev": "concurrently \"pnpm run dev:shared\" \"pnpm run dev:back\" \"pnpm run dev:front\"",
    "lag:up": "sudo tc qdisc add dev lo root netem delay 100ms",
    "lag:down": "sudo tc qdisc del dev lo root netem"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
